# План работ

Рабочий документ: что решено, что ещё не решено и что делать дальше.
Правится по ходу дела — это не история, история в `CHANGELOG.md`.

Отсчёт ведётся по версиям, а не по датам: версия однозначно говорит,
что уже в программе, а что нет.

**Как пользоваться.** Перед началом работы прочитать «Очередь работ»
и «Открытые вопросы». Взяли пункт — вычеркнули из очереди и записали
в `CHANGELOG.md` под своей версией. Появилось решение владельца —
записать в «Принятые решения» вместе с причиной: через полгода причина
дороже самого решения.

---

## Принятые решения

**Версия печатается везде** — в акте приёма, акте дефектации, акте
выполненных работ и на этикетке. Раньше было записано обратное («ни
в один документ не идут»); владелец решение изменил: одна и та же модель
в разных исполнениях — разное изделие, и заказчик должен видеть в акте,
какое именно ему привозили. Комментарий к версии («алюминиевый корпус»)
не печатается нигде — он нужен мастеру у стола.

**Версия — слитная приписка к названию модели**, одной строкой и одним
шрифтом: `БУАД-7-31.4`, `EcoDrive-2.3-1.1`, `МАГНУС-21.01`. Разделитель
хранится **внутри обозначения версии** (`.4`, `-1.1`), склейка — простое
сложение строк. Отдельного поля «разделитель» нет намеренно: на изделиях
разделители произвольные, и заполнять такое поле пришлось бы руками при
каждой версии — то есть ровно тот же труд, только в двух местах.

*Следствие:* обозначения, заведённые до перехода, надо просмотреть
руками. Автоматически не переделать — где кончается модель и начинается
исполнение, программа угадать не может.

**Родовое слово («Преобразователь частоты») в названии модели не живёт.**
Оно хранится отдельным полем `EquipmentModel.kind` и печатается только
в акте дефектации. В названии модели — короткое обозначение изделия
и ничего больше. Этикетка печатает название модели, поэтому лишние слова
в нём попадают на наклейку.

**Технологическая карта бывает без неисправности, и привязана она
к модели.** Решение владельца. «Как разобрать корпус» и «как проверить
на стенде» — про прибор, а не про поломку, и требовать для них
неисправность значило бы заводить пустышки в справочнике. Неисправность
у карты остаётся, но необязательной: указана — карта отвечает на эту
поломку, не указана — это общая процедура по модели.

*Следствие:* таблица «что к чему привязано» ниже поправлена — карта
висит на модели, а не на неисправности.

**Владельца оборудования при повторном приёме не меняем.**
`current_client` проставляется только пустой и не перезаписывается
никогда. Приезд от другого обслуживающего предприятия не значит, что
прибор сменил хозяина, а у кого он был в каждом ремонте, и так видно
по заказам.

**Изделие в коммерческом предложении печатается — с исполнением
и с возможностью правки.** У преобразователей частоты исполнения
отличаются мощностью, то есть и ценой ремонта, и заказчик должен видеть,
какой именно прибор ему считают. У приводов дверей исполнение на цену
почти не влияет, и там подробность только загромождает строку — поэтому
наименование правится руками, а пустое поле означает «печатай своё».
По умолчанию берётся полное обозначение с родовым словом, как в акте
дефектации: предложение и акт читает один человек.

**Страницы заказа переделываются: у единицы оборудования будет своя
страница.** Решение владельца. Строка в списке единиц становится
кликабельной и ведёт на страницу этой единицы: там основные данные,
документы, неисправности, детали, пломбы. Окно, а не страница, не годится
— окна рисует Bootstrap, а он приходит из интернета; страницу же можно
дать ссылкой, открыть в соседней вкладке и напечатать.

*Следствие:* **наклейка на приборе (`u/<id>`) ведёт на страницу единицы**,
а не на карточку заказа с подсветкой строки. Уже наклеенные наклейки
переклеивать не надо — в коде номер строки заказа, он не меняется.

**Страница правки заказа разбирается до конца, а не наполовину.**
Владелец назвал её громоздкой и неудобной. Оставленная «облегчённая»
правка означала бы два места для одних и тех же полей — ровно ту беду,
которую чиним; сейчас так уже вышло со статусом оплаты, он правится
и там, и своей формой на карточке, и вдобавок пересчитывается сам после
внесения платежа.

Шаблон формы при этом никуда не денется: на нём стоит приём заказа
(со своим коротким набором полей, с v2.57.0). Выгода не в удалённых
строках, а в исчезнувшем втором месте правки.

*Следствие:* в форме «принять ещё единицу» на карточке заказа появится
**кнопка «завести новое оборудование»** — решение владельца. Иначе прибор,
забытый при приёме, стало бы некуда добавить: заводить его пришлось бы
в справочнике оборудования отдельным заходом.

**Дефектация сохраняется кнопкой, а документ открывается своей.**
Сохранение уводило на печатный акт, и правка одной строки диагноза стоила
ухода на документ и возвращения назад. Сделано в v2.75.0.

**Скан открывает единицу оборудования в заказе**, а не заказ целиком.
История ремонтов этой единицы — ссылкой оттуда.

**Код в QR — короткий**, без адреса сервера: `o/123` вместо
`http://lifteam.taile9b605.ts.net/o/123`. Телефоном не сканируют, значит
ждать HTTPS и камеры в браузере не нужно. Модуль вырастает с 2,6 до 3,6
точки принтера, и внутренний адрес перестаёт печататься на наклейках,
которые уезжают к заказчику.

*Разбор со сканера к этому уже готов:* `scanning.decode` принимает
ведущую косую черту как необязательную, то есть `o/123` читается
и сегодня. Менять надо только то, что кладёт код в наклейку.

**Штрихкод вместо QR не подходит.** Тот же объём данных занимает вдвое
больше места и ложится длинной полосой поперёк наклейки, где места нет.
Главное же — у одномерного кода нет восстановления при повреждении:
царапина поперёк штрихов, флюс или спирт делают его нечитаемым, а QR при
уровне коррекции M переживает потерю до 15% площади. Единственный довод
за — дешёвый лазерный сканер, но он навсегда отрезает чтение телефоном,
а имиджер читает оба вида.

**Переименование в LiftTeam — только репозиторий на GitHub. Сделано:**
владелец переименовал его в настройках GitHub, адрес репозитория в файлах
исправлен ещё в v2.60.0, всё остальное, что видит человек, было написано
верно и раньше. Старый адрес продолжает работать — GitHub перенаправляет,
— так что клон на Raspberry Pi чинить не обязательно; захочется навести
порядок, это одна команда:
`git remote set-url origin https://github.com/yachtsman13/LiftTeam.git`.
На Raspberry Pi
переименования не будет: `/opt/lifteam`, пользователь `lifteam` и
четырнадцать юнитов systemd — это Unix-идентификаторы, а не название
программы (так же PostgreSQL живёт под пользователем `postgres`).
Переименование там означало бы окно простоя и риск сломать
самообновление, а увидеть его некому. Имя в Tailscale менять не нужно
в любом случае: в DNS регистр не различается.

**QR не уменьшать.** Место под другие строки берётся не из него.

**Значок вкладки — логотип целиком, без обрезки.** На 16 точках надпись
по кругу и микросхема сливаются, остаётся синий круг со светлой
окантовкой — этого достаточно: значку нужно быть узнаваемым среди
вкладок, а не читаемым. Обрезка до одной микросхемы проверена и оказалась
**хуже**: пропадает круглый контур и остаётся квадратное пятно.

---

## Открытые вопросы

Ждут решения владельца; без них соответствующую работу начинать нельзя.

*Открытых вопросов нет.* Появится — записать сюда до начала работы,
а решение владельца перенести в «Принятые решения» вместе с причиной.

---

## Очередь работ

Оценки — в токенах, по опыту предыдущих этапов.

| # | Работа | Оценка |
|---|---|---|
| 1 | Чистка справочника моделей — **делает владелец сам** | — |

Сделано: меню в один столбец, владелец оборудования из заказа, значок
вкладки (v2.59.0); переименование репозитория и версия слитной припиской
(v2.60.0); короткий код в QR и переход к единице заказа (v2.61.0);
скан старой наклейки при приёме (v2.62.0); приём по одной единице
с печатью наклейки (v2.63.0).

**Этап 4 закрыт целиком.** Подсказки по похожим серийникам в нём были
сделаны ещё раньше: при заведении оборудования программа предлагает уже
существующую запись, чтобы история ремонтов не разъехалась надвое.

Прайсы сделаны в v2.64.0. Цена привязана к **типу оборудования
и сложности**: сложность уже существовала, выводилась из неисправностей
и печаталась в предложении рядом с ценой — без неё простой и сложный
ремонт одного типа стоили бы одинаково.

Привязка деталей к единице сделана в v2.65.0 — по варианту Б, который
выбрал владелец: в заказе с одной единицей привязка проставляется сама,
с несколькими остаётся пустой, пока не укажут. Склад и себестоимость
не тронуты вовсе.

Возврат детали в партию сделан в v2.66.0. Партии разбираются в обратном
порядке — сначала та, из которой брали последней; возврат не создаёт новой
партии, а прирост склада делается уменьшением распределения. Себестоимость
правится отрицательной затратой, а не правкой прошлой записи.

Отложенное списание сделано в v2.66.1: запланированная деталь остаток
не трогает и в себестоимость не входит, а списывается тем же путём, что
обычная. **Пункт «детали на единицу» закрыт целиком.**

Обработчики скана по складским экранам сделаны в v2.67.0: пересчёт
инвентаризации, сетка кассетниц и карточка детали. Список радиодеталей
намеренно оставлен без подписки — там скан и так открывает нужную карточку.
Заодно сетка кассетниц перестала зависеть от того, приехал ли Bootstrap:
раньше отсутствие оформления гасило её скрипт целиком.

Материалы модели сделаны в v2.68.0: схемы, инструкции и методики —
ссылками у модели, с уточнением по исполнению, и кнопками на странице
дефектации. Хранится ссылка, а не файл; порядок видов значимый — схема
первой. Пункт «Яндекс.Диск целиком» это не закрывает: там нужна выгрузка
файлов через OAuth, а здесь ссылку копирует человек.

Технологические карты сделаны в v2.69.0 — без картинок: карта, шаги
с уточнением по исполнению, предостережения, печать на A4 и кнопки
на странице дефектации. Владелец решил, что карта бывает и без
неисправности и висит на модели; в очереди остались картинки к шагам,
и вместе с ними — включение `media/` в резервную копию.

Снимки к шагам сделаны в v2.70.0, вместе с ними — `media/` в резервной
копии. Снимок уменьшается до сохранения (1600 по длинной стороне),
PNG остаётся PNG, файл больше 15 МБ не принимается: это снимок
с телефона, а не схема. **Пункт «технологические карты» закрыт целиком.**

Готовность единицы сделана в v2.71.0 — **без поля состояния**: любое
такое поле проставляют руками и забывают, и оно начинает врать. Отметка
в строке, чек-лист на карточке заказа и предупреждение перед отгрузкой
читают один список проверок и потому всегда говорят одно и то же.
Запретов нет: программа называет недоделки вслух и пропускает.

Яндекс.Диск сделан в v2.72.0 — папкой заказа и ссылкой на неё. Загрузки
файлов через программу нет и не будет: снимки кладут телефоном, а он и так
умеет класть их на Диск. Публичной ссылки тоже нет — внутри оборудование
заказчика. **На живом Диске не проверено**: документация Яндекса из среды
разработки недоступна, схема написана по памяти и прогнана против
заглушки; первое обращение к настоящему Диску — под присмотром.

Камера сделана в v2.73.0: читает браузер (`BarcodeDetector`), результат
уходит в общий слой сканера, поэтому ни один экран не менялся. Своей
библиотеки нет — стороннее приезжает из интернета. Нужен HTTPS: сертификат
даёт Tailscale, со стороны программы правок не потребовалось.

Дефектация переделана в v2.75.0: сохранение оставляет на форме,
акт открывается своей кнопкой, а несохранённые правки названы строкой
рядом с ней — акт печатает то, что в базе, и разойтись они теперь могут.

Страница единицы сделана в v2.76.0: приём, диагностика, ремонт, детали
этой единицы, документы, папка на Диске, техкарты и материалы, гарантия
и история — в одном месте. Наклейка с прибора ведёт на неё. Строка
на карточке заказа стала кликабельной, длинные тексты в ней обрезаны,
столбец гарантии убран — из одобренных владельцем мелочей это те две,
что ложились внутрь этапа бесплатно.

Три мелочи по виду страниц сделаны в v2.77.0: смена статуса переехала
к значку статуса, в списке заказов появился столбец «Готовность»,
на дашборде — блок «Готово к отгрузке». Все три читают тот же список
проверок, что и чек-лист на карточке, поэтому говорить разное
об одном заказе они не могут.

Замечено попутно и **не чинилось**: список заказов делает по одному
запросу на заказ ради суммы (`total_repair_cost`). Это было и раньше;
свести в один запрос мешает то же, что и в выгрузке, — фильтры уже
соединяют таблицу с оборудованием, и сумма по такому соединению
задвоится. Чинить это стоит отдельно и с проверкой на настоящем списке.

Быстрые действия сделаны в v2.78.0: страница единицы правится на месте
(приём, неисправности, работы, пломбы, стоимость), в строке заказа
появились «записать работы» и «списать деталь», рецепт деталей
применяется со страницы единицы тем же обработчиком, что и из заказа.

Страница правки заказа разобрана в v2.79.0: заказчик и общее описание
правятся на карточке, единица снимается с заказа со своей страницы,
новое оборудование заводится обычной страницей с возвратом обратно.
Дубли номера счёта и статуса оплаты исчезли вместе с ней.

Замечания владельца по виду страниц заказа закрыты в v2.80.0: пробел
как разделитель модели и исполнения, отбор исполнений по модели, дата
акта дефектации по первой записи, фактическая стоимость в готовности
вместо оценки, чек-лист кнопками с переходом прямо в поле и порядком
по ходу работы.

Типовые выполненные работы сделаны в v2.81.0: у неисправности заведён
текст «что при этом делают», кнопка подставляет его в поле работ
на странице единицы и в поле предлагаемых работ в предложении. Именно
кнопкой, а не сборкой в акт: неисправности выбирают при дефектации,
до ремонта, и автоматическая сборка показала бы «работы записаны»
по неразобранному прибору.

**Очередь пуста.** Дальше — то, что назовёт владелец.

---

## Устройство: модель, версия, неисправности, карты

Правило одно и уже заложено в рецептах деталей: **что описывает прибор —
висит на модели; что описывает поломку — на неисправности; версия
ничего своего не заводит, она только уточняет.**

| Сущность | К чему привязана | Уточняется версией |
|---|---|---|
| Модель — `БУАД-7-31` | — | — |
| Версия — `.4` | модель | — |
| Единица (серийный номер) | модель + версия | — |
| Типовая неисправность | **модель** | **нет** — список общий |
| Строка рецепта деталей | неисправность | **да** — работает с v2.54.0 |
| Технологическая карта | **модель**; неисправность — по желанию | **да, шагом**, не картой целиком |
| Материалы: схемы, инструкции | **модель** | да, своими файлами |

**Почему неисправности висят на модели.** Иначе у каждого исполнения был
бы свой список поломок, а он общий: сохнут те же конденсаторы.

**Почему карта висит на модели, а неисправность у неё необязательна.**
Карта отвечает на вопрос «как это делают руками». Часть карт про поломку
(«нашли вот это — что делать»), часть — про сам прибор («как разобрать
корпус»). Карта на модель целиком была бы книгой, поэтому карт у модели
много, и каждая — процедура на полстраницы.

**Почему версия уточняет шаг, а не карту целиком.** При трёх исполнениях
вышло бы три почти одинаковых карты, и любую правку пришлось бы вносить
трижды.

**Наследования между исполнениями нет и заводить его нельзя.** Если
с картами это станет больно, ответ — не наследование, а **копирование
шагов из другого исполнения**, тем же приёмом, что уже сделан для
неисправностей и карточек деталей. Явная копия лучше неявной связи:
видно, что где стоит.

### Цепочка работы по заказу

1. **Приём** — единица: модель, версия, серийный номер. В акт приёма
   и на этикетку идёт обозначение `БУАД-7-31.4`.
2. **Диагностика** — выбираются типовые неисправности; список приходит
   от модели, одинаковый для всех исполнений.
3. **Детали** — программа собирает рецепт: общие строки плюс помеченные
   этим исполнением.
4. **Техкарта** той же неисправности — шаги, картинки, проверка
   на стенде; шаги с пометкой исполнения показываются только для него.
5. **Материалы модели** — схема, инструкция, методика — ссылкой рядом.
6. **Документы** — описание неисправности (не её короткое название)
   и обозначение изделия.

---

## Известные недоделки

*Форма версии («, исп. 1.1» вместо слитной приписки), меню в два столбца
и владелец оборудования из заказа — закрыты в v2.59.0 и v2.60.0.*

**Исполнения, заведённые до v2.60.0, надо просмотреть руками.** Раньше
обозначение хранилось без разделителя («2.0-1.1»), теперь разделитель
входит в него («-2.0-1.1»). Без правки обозначение склеится вплотную:
«БУАД-32.0-1.1». Автоматически не исправить — где кончается модель
и начинается исполнение, программа не знает.

**Записи без владельца, заведённые раньше, так и остались пустыми.**
Новые заполняются сами, старые — нет: программа не знает, от кого
приезжал прибор в прошлый раз. Проставить можно руками в карточке
оборудования либо при следующем приёме — он теперь проставит сам.

---

## Задачи владельца

Не программирование, но без них соответствующие пункты не работают.

- Внести белый IP в Т-Банке; выбрать имя для приёма уведомлений
  (A-запись `hooks.lifteam.ru` или DuckDNS); написать в `openapi@tbank.ru`
  и задать `WEBHOOKS_TBANK_SECRET`.
- Заменить заглушки локальной сети в `deploy/nginx-lifteam.conf`.
- Получить доступ к API Точки — без него её проверяющий отказывает всегда.
- Завести приложение OAuth для Яндекс.Диска и прописать
  `YANDEX_DISK_TOKEN` (DEPLOY.md, «Папка заказа на Яндекс.Диске»);
  первое нажатие кнопки «завести папку» проверить своими глазами.
- Купить **двумерный имиджер** (одномерный лазерный сканер QR не читает)
  и проверить его на настоящей наклейке 12,3 мм.
- Включить HTTPS ради камеры (DEPLOY.md, «HTTPS и камера») и проверить
  чтение кода камерой на настоящем планшете: распознаёт браузер,
  и в среде разработки его нет.
- Убедиться на первом живом счёте, что `invoice_external_id`
  заполняется, — без него уведомление об оплате не найдёт заказ.
